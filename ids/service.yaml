apiVersion: v1
kind: Service
metadata:
  name: ids-vnf
  namespace: testbed
  labels:
    app: ids-vnf
spec:
  selector:
    app: ids-vnf
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 7000
      protocol: TCP
      name: http