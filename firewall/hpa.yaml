apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: firewall-vnf
  namespace: testbed
  labels:
    app: firewall-vnf
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: firewall-vnf
  minReplicas: 1
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 20
